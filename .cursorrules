# Protos Web — Cursor entrypoint
#
# This repo is PRODUCTION CODE. Rules guard the codebase — they do not define the product.
# Full coding standards: .cursor/rules/*.mdc (alwaysApply)
# Long-term memory: Protos-Agent repo — NOT here.

Read first:
- .cursor/rules/protos-web.mdc      — stack, deploy, Supabase-only, repo roles
- .cursor/rules/dom-canvas-layers.mdc — showcase vs site chrome (mandatory for 3D/UI)
- .cursor/rules/fix-not-remove.mdc  — do not delete working features
- docs/architecture.md              — zones, backend ownership
- docs/security.md                  — secret placement

Memory / sessions / plans → ProtosEschatos/Protos-Agent (or /admin/memory). Do not duplicate in this repo.
