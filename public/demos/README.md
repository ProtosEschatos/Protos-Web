# Static demo bundles

Pre-built Vite output from [Protos-System-Boost](https://github.com/ProtosEschatos/Protos-System-Boost) served at `/demos/system-boost/`.

- Do not hand-edit minified `assets/*.js` — rebuild the source project and copy output here.
- Vendor bundles use a file-level `eslint-disable` header; ESLint still scans the repo without a global ignore.
