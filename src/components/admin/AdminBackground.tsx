/**
 * Flat admin backdrop — no decorative layers.
 */
export default function AdminBackground() {
  return (
    <div className="fixed inset-0 z-0 pointer-events-none bg-[#100818]" aria-hidden />
  )
}
