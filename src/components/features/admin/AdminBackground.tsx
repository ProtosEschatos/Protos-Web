/** Console backdrop — CSS only (reference: slate-950 + indigo radial). */
export default function AdminBackground() {
  return <div className="pointer-events-none fixed inset-0 z-0 bg-slate-950" aria-hidden />
}
